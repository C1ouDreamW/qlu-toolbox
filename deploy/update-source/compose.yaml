services:
  update-source:
    image: nginx:stable-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:18080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ${UPDATE_DATA_DIR:-./data}:/srv/updates:ro
