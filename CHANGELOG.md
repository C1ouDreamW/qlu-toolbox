# 更新日志

本项目版本遵循语义化版本号。Alpha 版本仍可能调整界面、配置格式和内部实现。

## [2.0.1] - 未发布

### 新增

- 桌面端新增「学分修读情况」工具：对照培养方案统计各模块学分修读进度，给出综合素质选修课修读建议，并支持自定义修读规则。感谢 [@LoMoCatAp](https://github.com/LoMoCatAp) 贡献（PR #7，原基于 v1.2.4，已适配 v2 架构）。
- Android 端新增「学分修读情况」工具：在学校页面手动登录后读取培养方案、最近 8 个学年成绩与最新学年在修课程，在本机统计综合素质选修课各模块学分、缺口及选课建议；支持艺术类专业豁免、学分要求调整与恢复默认，统计规则通过与桌面端共用样本验证。
- Android 学分统计会明确提示培养方案或在修名单读取失败；任一学期成绩查询失败时停止统计，避免展示不完整结果。

### 更改

- 桌面端课程编辑器的周次与时间选择对齐 Android 端交互：周数改为摘要行加锚定弹层（全选、单周、双周、清空一键预设与逐周宫格点选，保留 `1-8单`、`3,5,7` 表达式输入入口）；星期、开始节次与结束节次合并为一个弹层内的三列可循环滚轮（支持拖动、滚轮、方向键与点选，结束节次自动不早于开始节次），可将时段标记为“待安排”；编辑页对应行实时显示周次与上课时间的可读摘要。
- 新增共享包 `@lumatile/schedule-ui`：滚动轮换与周次摘要的纯逻辑由桌面端与 Android 端共用，两端选择行为保持一致。
- Android 端重构课程编辑器的周次与时间选择：周数由手写表达式输入改为底部弹层宫格点选，提供全选、单周、双周、清空一键预设，仍保留 `1-8单`、`3,5,7` 等表达式输入入口；星期、开始节次与结束节次合并为一个底部弹层内的三列可循环滚轮（支持拖动、点选、滚轮键与方向键，结束节次自动不早于开始节次），并可将时段标记为“待安排”；编辑页对应行实时显示周次与上课时间的可读摘要，时段复制、删除与冲突提示行为不变；系统返回键会先关闭弹层再逐级退出编辑页。
- Android versionCode 提升至 `11`，versionName 提升至 `2.0.1`，保持应用 ID 与签名配置不变。

## [2.0.0] - 2026-09-08

这是“一格有光 / LumaTile”品牌切换和自建更新源桥接版本。

### 新增

- 桌面端新增课表工具：周视图（今日列高亮、停课日覆盖、周末显示方式）、方向键/下拉切周、课程编辑器、多课表切换管理、开学日期与作息时间设置。
- 桌面端支持从教务系统导入课表：登录后点击学校原有的“输出EXCEL”，应用在同源环境接管导出并做大小与 SHA-256 校验；同时支持导入本地 .xls/.xlsx 课表。
- 桌面端支持课表备份导出与恢复（`<课表名>.lumatile-schedule.json`），可与 Android 端互相导入。
- 桌面端设置新增“启动页”选择（课表/首页/上次页面），与 Android 端一致。
- 桌面端设置“数据与隐私”新增“课表数据”条目，展示存储路径并支持一键定位文件。
- Android 端新增课表工具：周视图（今日列高亮）、滑动切周（带翻页动画，滑动时预览相邻一周）、课程编辑器、多课表切换管理、开学日期与作息时间设置（修改后自动保存）。
- Android 端支持从教务系统导入课表：在应用内登录教务并点击学校原有的“输出EXCEL”，由应用在同源环境接管导出并校验文件（需校园网环境）；同时支持从系统文件选择器导入 Excel 课表或 `.lumatile-schedule.json` 备份。
- Android 端支持将课表导出为 `<课表名>.lumatile-schedule.json` 并通过系统分享面板发送，可与桌面端互相导入。
- Android 端设置新增“应用启动页”选择（课表/工具箱/上次页面）。
- Android 端适配系统返回键：导入预览、课程详情、课程编辑器等课表层级的页面逐级返回，在首页 2 秒内再次按返回键则退出应用并给出提示。
- Android 与桌面端优先检查 `https://lumatile.ishua.cloud`，迁移期保留 GitHub 备用渠道。
- 发布流程生成可直接部署的静态更新目录，包含平台清单、安装包和校验文件。
- 新增 Docker Nginx 更新源及宿主机 Nginx 反向代理示例。
- 新增匿名使用统计与公告通道：客户端启动时向更新源发送匿名心跳（仅随机安装编号、版本和系统类型），并拉取更新源公告弹窗展示；服务器每日汇总访问日志生成下载量、日活和版本分布统计页（`/stats/`，Basic Auth 保护）。
- 桌面端与 Android 的「启动时检查更新」和「匿名使用统计」拆分为两个独立开关，可分别关闭；统计默认开启，可一键关闭。

### 更改

- 桌面端、Android、安装包和快捷方式的用户可见品牌切换为“一格有光 / LumaTile”，复用原方格图标。
- Android versionCode 提升至 `10`，versionName 提升至 `2.0.0`。

### 修复

- 修复 Android 15+ 设备上顶部栏被状态栏遮挡的问题：系统栏 insets 改由 `MainActivity` 原生统一接管（Android 15+ 给 WebView 补安全区 padding，传给网页的 safe-area insets 恒为 0），不再依赖设备 WebView 版本对 `env(safe-area-inset-*)` 的支持差异。
- 修复系统字体缩放导致 WebView 文字整体放大、课表格与顶栏布局被撑变形的问题（锁定 `textZoom` 为 100）。
- 修复旧版 System WebView（Chromium 84 之前，国产 ROM 出厂 WebView 常见）上卡片图标与文字贴在一起的问题：新增 flex 容器 `gap` 的运行时检测与 margin 兜底样式，仅在旧 WebView 上启用；同时将 `inset:0` 简写展开为四边定位。
- 修复双端课程编辑器无法识别手动输入周次表达式的问题，现已支持“1-8单”“3,5,7”等写法。
- 修复 Android 更新清单 `updates/android.json` 仍指向 v1.2.3 安装包的问题，已对齐 v1.2.4 正式发布包，保证存量客户端能完成最后一次 GitHub 桥接升级。

### 兼容性与迁移

- 保持 Android applicationId、正式签名证书、桌面 appId 和旧数据目录不变。
- v2.0.0 APK 仍需上传 GitHub Release，确保 v1.2.4 客户端能够完成最后一次 GitHub 桥接升级。

## [1.2.4] - 2026-07-19

### 更改

- 移除安卓端 `MainActivity` 与 `GradeExportActivity` 的 `FLAG_SECURE` 标志，恢复系统截图与录屏能力。
- 移除移动端首页顶部右上角的"Android Kotlin 测试版"角标。
- 移除设置页"检查更新"按钮副标题中的"双更新渠道"字样，仅保留当前版本号。

### 工程改进

- Android versionCode 提升至 `9`，versionName 提升至 `1.2.4`；后续 LumaTile 版本必须继续递增。

## [1.2.3] - 2026-07-19

这是 Android 端最终的“QLU 工具箱”迁移发布版本，替代此前未能完成发布流程的标签。

### 修复

- 移除在 Ubuntu runner 上反复误判的 APK 证书文本解析步骤；正式 APK 仍由 Gradle 强制使用配置的长期密钥库签名，构建成功后直接上传。

### 更改

- Android versionCode 提升至 `8`，versionName 提升至 `1.2.3`；后续 LumaTile 版本必须继续递增。

## [1.2.2] - 2026-07-19

这是一次发布流程修复尝试；该标签未能完成 Android 产物上传，已由 v1.2.3 替代。

### 修复

- 修复 Ubuntu runner 上重复导出密钥库证书并比较摘要时产生误判、阻止已正确签名 APK 上传的问题。
- 保留正式密钥库强制签名、`apksigner` 完整性验证、证书 SHA-256 读取和永久 applicationId 校验。

### 更改

- Android versionCode 提升至 `7`，versionName 提升至 `1.2.2`；后续 LumaTile 版本必须继续递增。

## [1.2.1] - 2026-07-19

这是一次发布流程修复尝试；该标签未能完成 Android 产物上传，已由 v1.2.2 替代。

### 修复

- 修复 Release 构建只更新 npm 版本、未将版本同步到 Python Worker，导致 macOS 冒烟测试失败的问题。
- 修复 Android 发布流程依赖手工录入证书指纹、可能因复制格式或取值错误而误判正式签名的问题；现在直接核对密钥库证书和 APK 证书。
- 修正 `setup-uv` 的 Python 版本参数，消除无效输入警告。

### 更改

- Android versionCode 提升至 `6`，versionName 提升至 `1.2.1`；后续 LumaTile 版本必须继续递增。

## [1.2.0] - 2026-07-19

这是 Android 端最后一个以“QLU 工具箱”为显示名称的迁移版本，为后续“一格有光 / LumaTile”品牌切换建立稳定的覆盖升级链。

### 新增

- Android 新增双更新清单机制，同时检查当前 `qlu-toolbox` 与未来 `lumatile` 仓库地址，单个地址不可用不会中断更新检查。
- 新增更新 APK 下载进度、文件大小和 SHA-256 校验，以及 applicationId、versionCode 和签名证书一致性校验。
- 新增 Android 正式签名配置、CI Debug 验证和 GitHub Actions 签名 Release 构建。
- Release 发布完成后自动生成 `updates/android.json`，确保清单只指向已经上传并完成校验的 APK。

### 更改

- Android 永久 applicationId 与 Kotlin namespace 改为 `io.github.c1oudreamw.lumatile`。
- Android versionCode 提升至 `5`；后续 LumaTile 版本必须继续递增。
- 移动端 npm workspace 作用域从 `@qlu-toolbox` 迁移为 `@lumatile`。
- 本版本继续显示“QLU 工具箱”并保留原图标，用户可见品牌将在后续大版本中切换。

### 兼容性与迁移

- 从 v1.2.3 Android 迁移版升级到 LumaTile 时，必须保持相同 applicationId 和签名证书，Android 才会覆盖安装并保留本地数据。
- 此前的 `cn.edu.qlu.toolbox` 测试包不能直接覆盖升级到本版本，需要先卸载旧测试包。
- 当前仓库暂不重命名；迁移版已经内置新旧两个更新地址，不依赖仓库重命名重定向。

## [1.1.0] - 2026-07-16

这是自 v1.0.3 以来的跨平台大版本更新，首次提供 Apple Silicon macOS 版本，并完善无可用系统浏览器时的备用方案。

### 新增

- 新增 Apple Silicon（arm64）macOS DMG，分项成绩导出和绩点计算器均可在 macOS 上使用。
- Edge 和 Chrome 均不可用时，可在应用内按需下载与当前 Playwright 版本匹配的备用 Chromium；安装完成后原导出任务自动继续。
- 设置页新增浏览器组件状态、版本、磁盘占用、文件位置和删除入口。
- GitHub Actions 新增 Apple Silicon 构建、Python Worker 启动、应用打包和 DMG 挂载验证。
- 正式发布流程同时生成 Windows 安装包、Windows 免安装 ZIP、Apple Silicon DMG 和统一的 SHA-256 校验文件。

### 更改

- 备用 Chromium 仅在首次需要时下载，不随安装包分发；组件保存在系统应用数据目录，升级软件时会保留。
- 浏览器组件下载支持进度显示、取消、失败重试和残留组件清理；取消时会终止相关下载进程。
- macOS 窗口改用左上角原生红黄绿控制按钮，Windows 继续使用现有窗口控制方式。
- macOS 设置、任务记录、日志、浏览器登录状态和备用浏览器统一保存在 `~/Library/Application Support/QLUToolbox`。
- README 新增 macOS 下载、升级、本地数据位置和 Apple 官方未知开发者应用打开教程。

### 修复

- 修复用户电脑未安装可用 Edge 或 Chrome 时，分项成绩查询无法正常启动的问题。
- 修复设置页打开数据位置时依赖 Windows 路径分隔符、在 macOS 上无法正确定位文件的问题。
- 修复浏览器组件下载取消或失败后，设置页不能及时显示残留文件和磁盘占用的问题。

### 兼容性与已知限制

- macOS 当前仅提供 Apple Silicon（arm64）版本，暂不支持 Intel Mac。
- macOS DMG 尚未使用 Apple 开发者证书签名和公证，首次启动可能需要按照 README 中的 Apple 官方教程手动确认“仍要打开”。
- 备用 Chromium 首次下载约 180 MiB，安装后约占用 350 MiB，需要能够访问 Playwright 浏览器下载服务。

## [1.0.3] - 2026-07-16

### 新增

- 分项成绩导出完成后新增"前往绩点计算器"引导卡片，支持一键跳转并导入刚导出的成绩。
- 关于页面 QQ 群号支持一键复制，适配 Electron、浏览器 API 和 DOM execCommand 多重剪贴板回退策略。

### 更改

- 新增统一下拉选择组件 BaseSelect，替代原生 `<select>`，支持键盘导航、级联焦点和无障碍访问。
- 设置页和分项成绩导出页全面使用 BaseSelect 组件。
- 整体字体调大（12/13/14px 体系），中文字体优先级提升，调整绿色和红色为更柔和的色调。
- 完善全局可读性样式，统一按钮、输入框和各页面排版节奏。

### 修复

- 修复绩点计算器取消勾选底部课程时界面白屏的问题。

## [1.0.2] - 2026-07-16

### 新增

- 新增绩点计算器，支持导入分项成绩 XLSX、展示全部成绩分项、自由勾选课程并实时计算总学分、总成绩点和加权平均 GPA。
- 无总评或成绩、学分无法识别的课程会保留展示并明确标记，不会自动纳入计算。
- 产品名称改为英文 `QLUToolbox.exe`，避免构建环境中文路径兼容问题。

### 修复

- 修复 GitHub Actions 发布工作流快捷方式创建失败的问题。

## [1.0.1] - 2026-07-16

### 全新界面

- 桌面 UI 从 PySide6 全面迁移到 Vue 3、TypeScript、Vite 与 Electron。
- 重做首页、工具中心、任务记录、设置、关于和分项成绩导出页面。
- 新增统一浅色/深色设计系统、响应式布局、页面转场和非阻塞通知。
- 保留原有 Python + Playwright 业务核心，通过安全 IPC 与独立 Worker 运行。

### 工程改进

- 新增长期运行的 Python Bridge，统一管理设置、任务数据库和后台任务。
- Electron 渲染进程启用上下文隔离、沙箱，并通过白名单预加载 API 使用系统能力。
- 新增 Vue/TypeScript 类型检查、前后端联合构建和 Electron 发布流程。

## [1.0.0] - 2026-07-15

首个正式发布版本，包含 v1.0.0-alpha.3 和 v1.0.0-alpha.4 中所有功能和修复。

## [1.0.0-alpha.4] - 2026-07-15

### 新增

- 任务记录页功能栏，统计展示任务总数及成功/失败/进行中数量。
- 任务记录空状态视图，无记录时显示引导提示。
- 设置页滚动区域，适配小窗口和低分辨率屏幕。
- 设置页数据管理面板，展示设置文件、任务记录、运行日志、登录状态和导出文件的完整路径，支持一键打开位置。
- 浏览器/主题设置并排网格布局，附带提示说明。
- 自定义下拉框组件（SettingsComboBox），自绘箭头图标，悬停和聚焦样式，弹出菜单圆角与选中高亮。
- 深色主题下数据路径（DataPath）等新控件的完整配色支持。

### 更改

- 任务记录表从 5 列简化为 4 列，合并"工具"和"参数"列，新增状态徽章和任务信息单元格的富界面渲染。
- 设置页保存时同步更新"导出文件"路径显示。
- README 新增"本地数据位置"章节，说明各数据目录路径及隐私提示。

### 修复

- 清除浏览器登录状态后可能残留部分 Cookie 数据的问题。
- 深色主题下状态徽章前景色不正确的问题。

### 测试

- 更新 UI 冒烟测试以覆盖新的表格列数、自定义下拉框、DataPath 字段和滚动区域。

## [1.0.0-alpha.3] - 2026-07-15

### 新增

- QLU 工具箱统一桌面框架、首页、工具列表、任务记录、设置和关于页面。
- 分项成绩导出工具，支持手动登录、学期校验、任务取消和结果打开。
- GitHub Release 更新提醒。
- 作者、问题反馈、安全与隐私报告渠道。
- uv 依赖管理、锁文件和 PyInstaller 目录式构建。

### 修复

- 修复 Excel 已生成但任务记录错误显示失败的问题。

### 说明

- 当前发布包尚未进行 Windows 代码签名。
