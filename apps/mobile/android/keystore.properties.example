# Copy this file to keystore.properties for local release builds.
# Never commit the real keystore or passwords.
storeFile=release-signing.jks
storePassword=replace-me
keyAlias=lumatile
keyPassword=replace-me
